apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-app-manager-webc.fullname" . }}
  labels:
    {{- include "kube-app-manager-webc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-app-manager-webc.selectorLabels" . | nindent 4 }}
